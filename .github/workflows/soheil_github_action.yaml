# .github/workflows/run-nginx.yml
name: Run Nginx Container

on:
  push:
    branches: ["main"]

jobs:
  run-docker:
    runs-on: self-hosted
    steps:
      - name: Run Nginx container
        run: docker run -d --name nginx-test -p 8080:80 nginx

    